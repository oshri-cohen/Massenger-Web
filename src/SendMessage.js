import './App.css';
import * as React from "react";

class SendMessage extends React.Component {
    render() {
        return (
            <div>
                this is the SendMessage
            </div>
        )
    }
}
export default SendMessage;
